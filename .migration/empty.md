# empty

2026-07-28 — golden pair via CLI (base-nova) + design-system replay — No radix dependency; refreshed to base-nova + design tokens.

## Changed

- `apps/web/src/frontend/components/ui/empty.tsx`.

Leftover scan clean: no `radix-ui` / `@radix-ui/react-*` (except icons) / `IconPlaceholder` / `asChild`.

## Left alone

- Empty states across notes/tasks/vault.

## Behavior changes

- None.

## Verify by hand

- Visit empty notes/tasks/vault lists.
